# 🚀 คู่มือด่วน: ตั้งค่า App Icon

## วิธีที่ 1: ใช้สคริปต์อัตโนมัติ (แนะนำ)

### Windows:
```
คลิกขวาที่ไฟล์ generate_icons.bat → Run as administrator
```

### macOS/Linux:
```bash
chmod +x generate_icons.sh
./generate_icons.sh
```

---

## วิธีที่ 2: รันคำสั่งเอง

```bash
# 1. ติดตั้ง dependencies
flutter pub get

# 2. สร้าง icons
flutter pub run flutter_launcher_icons

# 3. ตรวจสอบผลลัพธ์
ls android/app/src/main/res/mipmap-*/ic_launcher.*
```

---

## 📁 ไฟล์ Icon ที่ต้องมี

```
icon/icon.png  (ขนาด 1024x1024 px)
```

---

## ✅ ตรวจสอบว่าสำเร็จ

1. ไฟล์ icon ถูกสร้างใน `android/app/src/main/res/mipmap-*/`
2. ไฟล์ `colors.xml` มีสี `#003d7a`
3. ทดสอบ: `flutter run`

---

## 🔧 แก้ไขปัญหาเบื้องต้น

### Icon ไม่เปลี่ยน:
```bash
flutter clean
flutter pub get
flutter run
```

### ต้องการเปลี่ยนสีพื้นหลัง:
แก้ไข `pubspec.yaml` → `adaptive_icon_background: "#YOUR_COLOR"`

---

## 📖 คู่มือฉบับเต็ม

ดูรายละเอียดเพิ่มเติมที่: [ICON_SETUP_GUIDE.md](ICON_SETUP_GUIDE.md)

---

**หมายเหตุ:** ชื่อแอพคือ "RU ConneXt" (ดู AndroidManifest.xml)
